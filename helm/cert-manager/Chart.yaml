apiVersion: v2
name: cert-manager
description: A Helm chart for deploying cert-manager and Let's Encrypt ClusterIssuer via ArgoCD
type: application
version: 1.0.0
appVersion: "1.20.2"

dependencies:
  - name: cert-manager
    version: "v1.20.2"
    repository: "https://charts.jetstack.io"
    alias: cert-manager