# =============================================================================
# Ansible Collection Requirements
# =============================================================================
# Required Ansible collections for the EKS platform.
#
# Install with:
#   ansible-galaxy collection install -r collections/requirements.yml
# =============================================================================

collections:
  - name: kubernetes.core
    version: ">=2.4.0"